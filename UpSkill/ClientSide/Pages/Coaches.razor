@page "/Coaches"
<div class="text-right">
    <button @onclick="@(() => { NavigationManager.NavigateTo("/Coaches-catalog");})" class="btn-add">
        <div class="btn-text">
            Add
        </div>
    </button>
</div>

<div class="row">
    @for (int i = 1; i < 10; i++)
    {
    <CoachCard></CoachCard>
    }
</div>
@code
{
}
